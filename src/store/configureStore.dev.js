//a setup is shown here for the dev redux store configuration.
//Can be un-commenteded once required.
//import {createStore, applyMiddleware, compose} from 'redux';
//import rootReducer from '../reducers/index';
//import reduxImmutableStateInvariant from 'redux-immutable-state-invariant';
//import thunk from 'redux-thunk';

//export default function configureStore(initialState){
//    rootReducer,
//        initialState,
//        compose(applyMiddleware(thunk, reduxImmutableStateInvariant()),
            //window.devToolsExtension ? window.devToolsExtension() : f => f)
        //supplying middleware via commas as parameters. as much middleware as needed can be passed
//}